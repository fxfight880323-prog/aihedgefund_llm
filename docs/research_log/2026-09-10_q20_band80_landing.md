# 2026-09-10 · band80 落地到实盘 Q20·质衡优选

> 重要更正：之前误判"实盘 Q20 缺 band 层"是**错的**。
> 真正落地的是把 band80 做成可执行参数。

## 重要更正

`_bt_q20_kfin.py` 早已内置 band90 规避层（`WARN_PB=90`），8-27 建仓时已生效：
- q20 top40 里只剔除江苏银行（PB 分位 92.4%）1 只
- final 40 只 0 规避残留
- 之前 daily log 写"Q20 缺 band 层"是误判，**真正缺的是更严阈值 80%**。

## 本次落地（参数化）

| 文件 | 改动 |
|---|---|
| `_bt_q20_kfin.py` | 加 `--warn-pb` 参数（默认 90，可传 80），JSON 输出加 `warn_pb` 字段，HTML 硬编码 "90%" 改为变量 |
| `_sim_q20_engine.py` | docstring 同步更新（阈值可配置） |

## band80 vs band90 实证（实盘 juzi 数据源，as_of 2026-08-26）

| 阈值 | 剔除数 | 具体剔除的票 |
|---|---|---|
| band90（已生效）| 1 | 江苏银行 92.4% |
| **band80（更严）** | **+7** | 青岛银行 86.9% / 齐鲁银行 82.4% / 长江证券 87.9% / 焦作万方 87.0% / 中山公用 83.6% / 电投能源 85.5% / 新集能源 85.2% |

这 7 只全是"**横截面 PE 便宜但自身 PB 处 80-90% 历史高位**"的假便宜/周期高位/金融高位票。

替补池 29 只非规避可递补：中国银河 PB 16.4 / 广发证券 70.9 / 周大生 4.4 / 中信证券 54.5 / 盐湖股份 40.8 / 紫金矿业 / 等

## 决策（2027-04-30 调仓）

```bash
python _bt_q20_kfin.py --warn-pb 80    # 生成 band80 名单
python _sim_q20_rebalance.py            # 按新名单调仓
```

**依据**：
- 回测 band80：+135.0% / MDD 14.5% / 夏普 1.05
- 回测 band90：+129.9% / 15.6% / 1.03
- 当前截面（juzi 70 只）下额外剔除的 7 只正是"假便宜"票

**风险（诚实提示）**：
- band80 优势为样本内 + 回测 band 数据源（898 只快照）≠ 实盘（juzi 70 只）
- 调仓前（2027-04-30 前 1 周）建议再用最新数据跑一次回测，验证稳健性
- 若验证通过则用 band80，否则保守留在 band90
